---
import Icon from './Icon.astro';

export interface Props {
  icon?: 'microscope' | 'flask' | 'safety' | 'book' | 'lab' | 'test' | 'graduation' | 'research' | 'bacteria' | 'chemistry' | 'warning' | 'info' | 'check' | 'arrow-right' | 'arrow-left' | 'home' | 'search' | 'menu' | 'close';
  color?: 'blue' | 'green' | 'red' | 'purple' | 'yellow' | 'indigo' | 'gray';
  variant?: 'default' | 'gradient' | 'bordered' | 'elevated';
  className?: string;
}

const { 
  icon,
  color = 'blue',
  variant = 'bordered',
  className = ''
} = Astro.props;

const colorClasses = {
  blue: {
    bg: 'bg-blue-50',
    border: 'border-blue-200',
    borderLeft: 'border-l-blue-400',
    gradient: 'from-blue-50 to-blue-100',
    text: 'text-blue-900',
    iconBg: 'bg-blue-600'
  },
  green: {
    bg: 'bg-green-50',
    border: 'border-green-200',
    borderLeft: 'border-l-green-400',
    gradient: 'from-green-50 to-green-100',
    text: 'text-green-900',
    iconBg: 'bg-green-600'
  },
  red: {
    bg: 'bg-red-50',
    border: 'border-red-200',
    borderLeft: 'border-l-red-400',
    gradient: 'from-red-50 to-red-100',
    text: 'text-red-900',
    iconBg: 'bg-red-600'
  },
  purple: {
    bg: 'bg-purple-50',
    border: 'border-purple-200',
    borderLeft: 'border-l-purple-400',
    gradient: 'from-purple-50 to-purple-100',
    text: 'text-purple-900',
    iconBg: 'bg-purple-600'
  },
  yellow: {
    bg: 'bg-yellow-50',
    border: 'border-yellow-200',
    borderLeft: 'border-l-yellow-400',
    gradient: 'from-yellow-50 to-yellow-100',
    text: 'text-yellow-900',
    iconBg: 'bg-yellow-600'
  },
  indigo: {
    bg: 'bg-indigo-50',
    border: 'border-indigo-200',
    borderLeft: 'border-l-indigo-400',
    gradient: 'from-indigo-50 to-indigo-100',
    text: 'text-indigo-900',
    iconBg: 'bg-indigo-600'
  },
  gray: {
    bg: 'bg-gray-50',
    border: 'border-gray-200',
    borderLeft: 'border-l-gray-400',
    gradient: 'from-gray-50 to-gray-100',
    text: 'text-gray-900',
    iconBg: 'bg-gray-600'
  }
};

const variantClasses = {
  default: `${colorClasses[color].bg} ${colorClasses[color].border} border rounded-lg shadow-sm`,
  gradient: `bg-gradient-to-br ${colorClasses[color].gradient} border ${colorClasses[color].border} rounded-lg shadow-md`,
  bordered: `${colorClasses[color].bg} border-l-4 ${colorClasses[color].borderLeft} border-r border-t border-b ${colorClasses[color].border} rounded-r-lg shadow-sm`,
  elevated: `${colorClasses[color].bg} ${colorClasses[color].border} border rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-300`
};
---

<div class={`${variantClasses[variant]} p-6 group hover:scale-[1.02] transition-all duration-300 ${className}`}>
  <div class="flex items-start space-x-4">
    {icon && (
      <div class="flex-shrink-0">
        <div class={`w-12 h-12 ${colorClasses[color].iconBg} rounded-lg flex items-center justify-center group-hover:scale-110 transition-transform duration-300`}>
          <Icon name={icon} size="lg" color="white" />
        </div>
      </div>
    )}
    <div class="flex-1">
      <p class="text-lg leading-relaxed text-gray-700 group-hover:text-gray-800 transition-colors duration-300">
        <slot />
      </p>
    </div>
  </div>
</div>
