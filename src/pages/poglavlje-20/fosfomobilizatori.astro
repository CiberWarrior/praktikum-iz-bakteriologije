---
import SubPageLayoutWithBase from "../../components/SubPageLayoutWithBase.astro";

const title = 'Fosfomobilizatori';
const description = 'Bakterije koje mobiliziraju kemijski fiksirani fosfor do ortofosfata';
---

<SubPageLayoutWithBase chapterNumber={20} 
  title={title}
  description={description}
  chapterNumber={20}
  primaryColor="teal"
  prevLink="/poglavlje-20/fosfat-akumulirajuce-bakterije/"
  prevText="← Fosfat-akumulirajuće bakterije"
  nextLink="/poglavlje-20/vjezba-20/"
  nextText="Vježbe 20 →"
>

  <!-- Main Content with Sidebar -->
  <div class="flex max-w-7xl mx-auto">
    
    <!-- Table of Contents Sidebar -->
    <aside class="hidden lg:block w-64 p-6 bg-gradient-to-b from-gray-50 to-gray-100 border-r border-gray-200 sticky top-0 h-screen overflow-y-auto shadow-lg">
      <div class="mb-6">
        <div class="flex items-center mb-6">
          <div class="w-8 h-8 bg-teal-600 rounded-lg flex items-center justify-center mr-3">
            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900">Sadržaj stranice</h3>
        </div>
        
        <!-- Bookmark Button -->
        <button id="bookmark-btn" class="flex items-center space-x-3 text-sm text-gray-600 hover:text-teal-600 transition-all duration-300 mb-6 p-3 rounded-lg hover:bg-teal-50 w-full">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path>
          </svg>
          <span id="bookmark-text" class="font-medium">Označiti kao bookmark</span>
        </button>
        
        <!-- Table of Contents -->
        <nav class="space-y-1">
          <div class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-2">Sadržaj stranice</div>
          <a href="#karakteristike" class="toc-link group flex items-center text-sm text-gray-600 hover:text-teal-600 transition-all duration-300 py-2 px-3 rounded-lg hover:bg-teal-50" data-target="karakteristike">
            <span class="w-2 h-2 bg-orange-400 rounded-full mr-3 transition-all duration-300"></span>
            Karakteristike fosfomobilizatora
          </a>
          <a href="#sastav-podloge" class="toc-link group flex items-center text-sm text-gray-600 hover:text-teal-600 transition-all duration-300 py-2 px-3 rounded-lg hover:bg-teal-50" data-target="sastav-podloge">
            <span class="w-2 h-2 bg-blue-400 rounded-full mr-3 transition-all duration-300"></span>
            Sastav podloge
          </a>
          <a href="#mehanizam" class="toc-link group flex items-center text-sm text-gray-600 hover:text-teal-600 transition-all duration-300 py-2 px-3 rounded-lg hover:bg-teal-50" data-target="mehanizam">
            <span class="w-2 h-2 bg-green-400 rounded-full mr-3 transition-all duration-300"></span>
            Mehanizam mobilizacije
          </a>
          <a href="#rezultat" class="toc-link group flex items-center text-sm text-gray-600 hover:text-teal-600 transition-all duration-300 py-2 px-3 rounded-lg hover:bg-teal-50" data-target="rezultat">
            <span class="w-2 h-2 bg-purple-400 rounded-full mr-3 transition-all duration-300"></span>
            Rezultat mobilizacije
          </a>
        </nav>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 px-6 py-8">
      <div class="max-w-4xl mx-auto">
        <div class="prose prose-lg max-w-none">
          
          <!-- Content Blocks -->
          <div class="space-y-8">
            
            <!-- Karakteristike fosfomobilizatora -->
            <div id="karakteristike" class="bg-gradient-to-br from-orange-50 to-amber-50 rounded-xl p-6 shadow-lg border border-orange-200">
              <div class="flex items-center mb-4">
                <span class="text-3xl mr-4">⚡</span>
                <h3 class="text-2xl font-bold text-orange-900">Karakteristike fosfomobilizatora</h3>
              </div>
              <p class="text-gray-700 leading-relaxed mb-4">
                Fiziološka skupina fosfomobilizirajućih bakterija sposobna je produciranim anorganskim ili organskim kiselinama (tzv. kiselinski tipovi bakterija) mobilizirati kemijski fiksirani fosfor u sedimentima do ortofosfata (PO₄³⁻) te vraćati fosfor u ciklus kruženja u prirodi.
              </p>
              <p class="text-gray-700 leading-relaxed">
                Fosfomobilizirajuće bakterije uzgajaju se na krutoj hranjivoj podlozi uz dodatak kalcijeva fosfata (Ca₃(PO₄)₂) koji daje bijelo obojenje podloge.
              </p>
            </div>

            <!-- Sastav podloge -->
            <div id="sastav-podloge" class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl p-6 shadow-lg border border-blue-200">
              <div class="flex items-center mb-4">
                <span class="text-3xl mr-4">🧪</span>
                <h3 class="text-2xl font-bold text-blue-900">Sastav podloge za fosfomobilizatore</h3>
              </div>
              
              <div class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl p-6 border border-blue-200">
                <div class="flex items-center mb-4">
                  <span class="text-2xl mr-3">🧪</span>
                  <h5 class="text-lg font-semibold text-blue-900">Sastav podloge za fosfomobilizatore</h5>
                </div>
                <div class="bg-white rounded-lg p-4 border border-blue-200">
                  <div class="space-y-3">
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">Glukoza</span>
                      <span class="bg-blue-500 text-white px-3 py-1 rounded-full text-sm font-semibold">20,0 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">Ca₃(PO₄)₂</span>
                      <span class="bg-orange-500 text-white px-3 py-1 rounded-full text-sm font-semibold">5,0 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">(NH₄)₂SO₄</span>
                      <span class="bg-red-500 text-white px-3 py-1 rounded-full text-sm font-semibold">0,5 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">MgCl₂</span>
                      <span class="bg-purple-500 text-white px-3 py-1 rounded-full text-sm font-semibold">0,3 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">NaCl</span>
                      <span class="bg-pink-500 text-white px-3 py-1 rounded-full text-sm font-semibold">0,2 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">KCl</span>
                      <span class="bg-green-500 text-white px-3 py-1 rounded-full text-sm font-semibold">0,1 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">FeSO₄, MgSO₄</span>
                      <span class="bg-yellow-500 text-white px-3 py-1 rounded-full text-sm font-semibold">tragovi</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">Agar</span>
                      <span class="bg-emerald-500 text-white px-3 py-1 rounded-full text-sm font-semibold">20,0 g</span>
                    </div>
                    <div class="flex justify-between items-center">
                      <span class="font-medium text-gray-700">Destilirana voda</span>
                      <span class="bg-cyan-500 text-white px-3 py-1 rounded-full text-sm font-semibold">1000 mL</span>
                    </div>
                  </div>
                  <div class="mt-4 pt-4 border-t border-gray-200">
                    <div class="flex justify-center">
                      <span class="bg-violet-500 text-white px-4 py-2 rounded-full text-sm font-semibold">pH = 7,0 ± 0,1</span>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Mehanizam mobilizacije -->
            <div id="mehanizam" class="bg-gradient-to-br from-green-50 to-emerald-50 rounded-xl p-6 shadow-lg border border-green-200">
              <div class="flex items-center mb-4">
                <span class="text-3xl mr-4">⚙️</span>
                <h3 class="text-2xl font-bold text-green-900">Mehanizam mobilizacije fosfora</h3>
              </div>
              <p class="text-gray-700 leading-relaxed mb-4">
                Kiselinski tipovi bakterija razgrađuju glukozu u podlozi do kiselina, a nastale kiseline reagiraju s Ca₃(PO₄)₂ te ga prevode do ortofosfata (mobiliziraju).
              </p>
              <div class="bg-white rounded-lg p-4 shadow-sm">
                <h4 class="font-semibold text-green-800 mb-3">Proces mobilizacije:</h4>
                <p class="text-gray-700 text-center font-medium">
                  Glukoza → Kiseline → Mobilizacija Ca₃(PO₄)₂ → PO₄³⁻
                </p>
              </div>
            </div>

            <!-- Rezultat mobilizacije -->
            <div id="rezultat" class="bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl p-6 shadow-lg border border-purple-200">
              <div class="flex items-center mb-4">
                <span class="text-3xl mr-4">🎯</span>
                <h3 class="text-2xl font-bold text-purple-900">Rezultat mobilizacije</h3>
              </div>
              
              <p class="text-gray-700 leading-relaxed mb-4">
                Oko kolonija koje su mobilizirale fosfor iz Ca₃(PO₄)₂ nastaje prozirna zona (slika 85).
              </p>

              <figure class="image-figure bg-white rounded-lg p-4 shadow-sm mt-6">
                <img src="/Images/slika85.jpg" alt="Kolonije bakterija fosfomobilizatora izolirane na podlozi za fosfomobilizatore" class="w-3/5 h-auto rounded-lg mx-auto" />
                <figcaption class="mt-4 text-center">
                  <span class="inline-block bg-purple-100 text-purple-800 px-3 py-1 rounded-full text-sm font-medium">Slika 85</span>
                  <span class="block mt-2 text-gray-700">Kolonije bakterija fosfomobilizatora izolirane na podlozi za fosfomobilizatore</span>
                </figcaption>
              </figure>
            </div>

          </div>

        </div>
      </div>
    </main>
  </div>

  <!-- Mobile TOC Toggle -->
  <button id="mobile-toc-toggle" class="lg:hidden fixed bottom-4 right-4 bg-teal-600 text-white p-3 rounded-full shadow-lg z-50">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"></path>
    </svg>
  </button>

  <!-- Mobile TOC Overlay -->
  <div id="mobile-toc-overlay" class="lg:hidden fixed inset-0 bg-black bg-opacity-50 z-40 hidden">
    <div class="fixed right-0 top-0 h-full w-80 bg-white shadow-xl transform translate-x-full transition-transform duration-300" id="mobile-toc-panel">
      <div class="p-6">
        <div class="flex justify-between items-center mb-6">
          <h3 class="text-lg font-semibold text-gray-900">Brza navigacija</h3>
          <button id="mobile-toc-close" class="text-gray-500 hover:text-gray-700">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
        
        <!-- Bookmark Button -->
        <button id="mobile-bookmark-btn" class="flex items-center space-x-2 text-sm text-gray-600 hover:text-teal-600 transition-colors duration-200 mb-4 w-full">
          <svg id="mobile-bookmark-icon" class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path>
          </svg>
          <span id="mobile-bookmark-text">Označiti kao bookmark</span>
        </button>
        
        <!-- Navigation Links -->
        <nav class="space-y-2">
          <a href="/poglavlje-20/" class="mobile-toc-link block text-sm text-gray-600 hover:text-teal-600 transition-colors duration-200 py-2 border-l-2 border-transparent hover:border-teal-600 pl-3">
            Pregled poglavlja
          </a>
          <a href="/sadrzaj/" class="mobile-toc-link block text-sm text-gray-600 hover:text-teal-600 transition-colors duration-200 py-2 border-l-2 border-transparent hover:border-teal-600 pl-3">
            Svi sadržaji
          </a>
        </nav>
      </div>
    </div>
  </div>

  <!-- Navigacijski gumbovi -->
  <div class="flex justify-between mt-16 mb-8 max-w-4xl mx-auto px-4 sm:px-6">
    <!-- Prethodna stranica -->
    <a
      href="/poglavlje-20/fosfat-akumulirajuce-bakterije/"
      class="inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-gray-600 to-gray-700 text-white font-semibold rounded-full shadow-lg hover:shadow-xl transition-all duration-300 no-underline transform hover:-translate-y-1"
      aria-label="Prijeđi na prethodnu stranicu: fosfat-akumulirajuće bakterije"
    >
      ←
      <span class="text-lg">fosfat-akumulirajuće bakterije</span>
    </a>
    
    <!-- Iduća stranica -->
    <a
      href="/poglavlje-20/vjezba-20/"
      class="inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-teal-600 to-teal-700 text-white font-semibold rounded-full shadow-lg hover:shadow-xl transition-all duration-300 no-underline transform hover:-translate-y-1"
      aria-label="Prijeđi na sljedeću stranicu: vježbe 20"
    >
      <span class="text-lg">vježbe 20</span>
      →
    </a>
  </div>

  <!-- CSS Styles for Active State -->
  <style>
    .toc-link.active {
      @apply text-teal-600 bg-teal-50 font-semibold;
    }
    
    .toc-link.active span {
      @apply w-3 h-3 scale-125;
    }
  </style>

  <!-- JavaScript -->
  <script>
    // Bookmark functionality
    function initBookmarks() {
      const bookmarkKey = 'bookmark-fosfomobilizatori';
      const bookmarkBtn = document.getElementById('bookmark-btn');
      const bookmarkIcon = document.getElementById('bookmark-icon');
      const bookmarkText = document.getElementById('bookmark-text');
      const mobileBookmarkBtn = document.getElementById('mobile-bookmark-btn');
      const mobileBookmarkIcon = document.getElementById('mobile-bookmark-icon');
      const mobileBookmarkText = document.getElementById('mobile-bookmark-text');
      
      // Check if bookmarked
      const isBookmarked = localStorage.getItem(bookmarkKey) === 'true';
      updateBookmarkUI(isBookmarked);
      
      function updateBookmarkUI(bookmarked: boolean) {
        const fillAttribute = bookmarked ? 'currentColor' : 'none';
        const text = bookmarked ? 'Ukloniti bookmark' : 'Označiti kao bookmark';
        
        if (bookmarkIcon) bookmarkIcon.setAttribute('fill', fillAttribute);
        if (bookmarkText) bookmarkText.textContent = text;
        if (mobileBookmarkIcon) mobileBookmarkIcon.setAttribute('fill', fillAttribute);
        if (mobileBookmarkText) mobileBookmarkText.textContent = text;
      }
      
      function toggleBookmark() {
        const currentlyBookmarked = localStorage.getItem(bookmarkKey) === 'true';
        const newBookmarkState = !currentlyBookmarked;
        localStorage.setItem(bookmarkKey, newBookmarkState.toString());
        updateBookmarkUI(newBookmarkState);
      }
      
      if (bookmarkBtn) bookmarkBtn.addEventListener('click', toggleBookmark);
      if (mobileBookmarkBtn) mobileBookmarkBtn.addEventListener('click', toggleBookmark);
    }

    // Mobile TOC
    function initMobileTOC() {
      const toggleBtn = document.getElementById('mobile-toc-toggle');
      const overlay = document.getElementById('mobile-toc-overlay');
      const panel = document.getElementById('mobile-toc-panel');
      const closeBtn = document.getElementById('mobile-toc-close');
      const tocLinks = document.querySelectorAll('.mobile-toc-link');
      
      function openTOC() {
        if (overlay && panel) {
          overlay.classList.remove('hidden');
          setTimeout(() => {
            panel.classList.remove('translate-x-full');
          }, 10);
        }
      }
      
      function closeTOC() {
        if (panel && overlay) {
          panel.classList.add('translate-x-full');
          setTimeout(() => {
            overlay.classList.add('hidden');
          }, 300);
        }
      }
      
      if (toggleBtn) toggleBtn.addEventListener('click', openTOC);
      if (closeBtn) closeBtn.addEventListener('click', closeTOC);
      if (overlay) {
        overlay.addEventListener('click', (e) => {
          if (e.target === overlay) closeTOC();
        });
      }
      
      // Close TOC when clicking on a link
      tocLinks.forEach(link => {
        link.addEventListener('click', closeTOC);
      });
    }

    // Scroll Spy for TOC
    function initScrollSpy() {
      const sections = document.querySelectorAll('[id^="karakteristike"], [id^="sastav-"], [id^="mehanizam"], [id^="rezultat"]');
      const tocLinks = document.querySelectorAll('.toc-link');
      
      function updateActiveLink() {
        let currentSection = '';
        
        sections.forEach(section => {
          const rect = section.getBoundingClientRect();
          if (rect.top <= 100 && rect.bottom >= 100) {
            currentSection = section.id;
          }
        });
        
        // Remove active class from all links
        tocLinks.forEach(link => {
          link.classList.remove('active');
        });
        
        // Add active class to current section link
        if (currentSection) {
          const activeLink = document.querySelector(`[data-target="${currentSection}"]`);
          if (activeLink) {
            activeLink.classList.add('active');
          }
        }
      }
      
      // Listen for scroll events
      window.addEventListener('scroll', updateActiveLink);
      
      // Initial check
      updateActiveLink();
    }

    // Initialize everything
    document.addEventListener('DOMContentLoaded', () => {
      initBookmarks();
      initMobileTOC();
      initScrollSpy();
    });
  </script>

</SubPageLayoutWithBase>